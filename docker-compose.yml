services:
  agent:
    build: ./agent
    container_name: agent
    expose:
      - "7000"
    networks:
      - mesh
    environment:
      - GATEWAY_URL=http://gateway:9000
      - CAPABILITY_SECRET=${CAPABILITY_SECRET:-dev-secret}
      - CAP_ISS=broker
      - CAP_AUD=agent
    user: "1000:1000"
    restart: unless-stopped

  broker:
    build: 
      context: ./broker
      args:
        ENABLE_LLM_BUILD: ${ENABLE_LLM_BUILD:-false}
    container_name: broker
    ports:
      - "8001:8001"
    networks:
      - mesh
      - public
    environment:
      - PORT=8001
      - BROKER_API_KEY=${BROKER_API_KEY:-DEMO-KEY}
      - CAPABILITY_SECRET=${CAPABILITY_SECRET:-dev-secret}
      - ENABLE_LLM_FIREWALL=${ENABLE_LLM_FIREWALL:-true}
    volumes:
      - ./broker/data:/app/data
    restart: unless-stopped

  gateway:
    build: ./gateway
    container_name: gateway
    ports:
      - "9000:9000"
    networks:
      - mesh
      - public
    environment:
      - PORT=9000
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - ANTHROPIC_MODEL=${ANTHROPIC_MODEL:-claude-3-5-sonnet-latest}
    volumes:
      - ./gateway/data:/app/data
    restart: unless-stopped

  agent-langgraph:
    build: ./agent-langgraph
    container_name: agent-langgraph
    ports:
      - "8003:8003"
    networks:
      - mesh
      - public
    environment:
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - AWS_REGION=${AWS_REGION:-us-east-1}
      - LLM_MODEL=${LLM_MODEL:-anthropic.claude-3-5-sonnet-20240620-v1:0}
      - LLM_TEMPERATURE=0.1
      - LLM_MAX_TOKENS=1000
      - BANKING_API_URL=http://banking-api:8004
      - BANKING_API_KEY=${BANKING_API_KEY:-BANKING-API-KEY-123}
    restart: unless-stopped
    depends_on:
      - banking-api

  banking-api:
    build: ./banking-api
    container_name: banking-api
    ports:
      - "8004:8004"
    networks:
      - mesh
      - public
    environment:
      - BANKING_API_KEY=${BANKING_API_KEY:-BANKING-API-KEY-123}
      - BANKING_API_PORT=8004
      - MONGODB_URL=${MONGODB_URL}
      - DATABASE_NAME=${DATABASE_NAME:-banking_db}
    restart: unless-stopped

networks:
  mesh:
    internal: true
  public:
    driver: bridge